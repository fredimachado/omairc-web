node_modules/
.astro/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Astro build output. install.sh is fetched from the Omairc repository during
# deployment, so a local copy here would only ever be stale.
dist/
